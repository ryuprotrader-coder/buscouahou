---
import '../styles/globals.css';
import AuthCookieSync from '../components/auth/AuthCookieSync';
import ClientHeader from '../components/layout/ClientHeader';
import ClientBottomNav from '../components/layout/ClientBottomNav';

interface Props {
  title: string;
  hideHeader?: boolean;
}

const { title, hideHeader = false } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="pt-BR" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="description" content={`${title} — Mais Melhor Marketplace`} />
    <meta name="theme-color" content="#FF6B00" />
    <title>{title} · Mais Melhor</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    
    <!-- For mobile PWA-like feeling -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  </head>
  <body class="bg-neutral-50 dark:bg-neutral-950 text-neutral-900 dark:text-neutral-50 antialiased selection:bg-brand-500/30 selection:text-brand-900 dark:selection:text-brand-100 overflow-x-hidden">
    <AuthCookieSync client:load />
    <!-- Main Client Layout Container -->
    <div class="flex flex-col h-[100dvh] max-w-md mx-auto relative bg-white dark:bg-neutral-950 shadow-2xl xl:border-x xl:border-neutral-200 dark:xl:border-neutral-800">
      
      <!-- Glass Header overlaid on top -->
      {!hideHeader && (
        <div class="absolute top-0 inset-x-0 z-30">
          <ClientHeader client:load />
        </div>
      )}

      <!-- Content Area scrolls underneath header -->
      <main class={`flex-1 overflow-y-auto no-scrollbar relative w-full pb-[calc(env(safe-area-inset-bottom)+70px)] ${!hideHeader ? 'pt-[calc(env(safe-area-inset-top)+140px)]' : ''}`}>
        <slot />
      </main>

      <!-- Bottom Navigation -->
      <div class="absolute bottom-0 inset-x-0 z-40">
        <ClientBottomNav activePath={currentPath} client:load />
      </div>
    </div>

    <style is:global>
      /* Hide scrollbar for Chrome, Safari and Opera */
      .no-scrollbar::-webkit-scrollbar {
        display: none;
      }
      /* Hide scrollbar for IE, Edge and Firefox */
      .no-scrollbar {
        -ms-overflow-style: none;  /* IE and Edge */
        scrollbar-width: none;  /* Firefox */
      }
    </style>
  </body>
</html>
